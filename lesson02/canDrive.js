const first = 'Kelly';
const last = 'Ros';
let age = 16;


if (age >= 16) {
    console.log(first + ' ' + last + ' has been driving for' + (16-age) + ' years.');
}else{
    for (let i=5; i>=0; i--){
        console.log(i);
    }
}
